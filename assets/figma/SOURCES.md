# 출처

이 디렉토리의 모든 SVG/사진은 팀 SYNC의 Figma 디자인 파일(Feellog v6 블루 DS)에서 Figma REST API로 추출한 것이다 (`figma-export/` 참조, 원본 노드id는 `src/assets/figmaAssets.ts` 주석에 기록). 사진(photos/)은 총량 관리를 위해 `sips -Z 1200`, JPEG 품질 70으로 압축했다.
